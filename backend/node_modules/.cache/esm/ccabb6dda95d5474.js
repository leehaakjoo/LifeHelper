let Post;_a95‍.x([["default",()=>createFakeData]]);_a95‍.w("./models/post",[["default",["Post"],function(v){Post=v}]]);

               function createFakeData(){
    const posts = [...Array(40).keys()].map( i => ({
        title: `포스트 #${i}`,
        body: `${i} 번째 포스트 입니다`,
        comments:{
            body : '댓글',
        },
    }));
    Post.insertMany(posts, (err, docs) => {
        _a95‍.g.console.log(docs);
    });
}