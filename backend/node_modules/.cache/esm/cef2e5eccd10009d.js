_cd1‍.x([["write",()=>write],["list",()=>list],["read",()=>read],["remove",()=>remove],["update",()=>update]]);const Post = require('../../models/post');

       const write = async ctx => {
    const {title, body} = ctx.request.body;
    const post = new Post({
        title,
        body,
    });
    try{
        await post.save();
        ctx.body = post;
    } catch (e) {
        ctx.throw(500, e);
    }
};

       const list = ctx => {

};
       const read = ctx => {

};
       const remove = ctx => {

};
       const update = ctx => {

};
